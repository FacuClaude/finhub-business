<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="color-scheme" content="light" />
  <title>finHub Business · Acceder</title>
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@300;400;500;600;700&family=DM+Sans:wght@300;400;500&display=swap" rel="stylesheet" />
  <style>
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

    html { color-scheme: light only; }

    :root {
      /* ── Neutros (idénticos a finHub home) ── */
      --bg:      #f5f3f0;
      --surface: #fdfaf4;
      --border:  #e0d8cc;
      --ink:     #1a1a14;
      --ink2:    #3a3830;
      --muted:   #8a8070;

      /* ── Accent azul corporativo (reemplaza el verde) ── */
      --accent:     #1e4d8c;
      --accent-mid: #2460a7;
      --accent-dim: rgba(30,77,140,0.10);

      /* ── Acento secundario: azul-pizarra para highlights ── */
      --accent2:    #3b7dd8;

      --error:   #b85050;
      --font-ui: 'DM Sans', ui-sans-serif, -apple-system, BlinkMacSystemFont, sans-serif;
      --font-hd: 'Plus Jakarta Sans', ui-sans-serif, sans-serif;
    }

    html, body { height: 100%; }

    body {
      background: var(--bg) !important;
      color: var(--ink) !important;
      font-family: var(--font-ui);
      font-weight: 300;
    }

    /* Textura de puntos, igual que finHub home */
    body::before {
      content: '';
      position: fixed; inset: 0;
      background-image: radial-gradient(circle, #ccc4b4 1px, transparent 1px);
      background-size: 28px 28px;
      opacity: 0.4;
      pointer-events: none;
    }

    /* ── LAYOUT ── */
    .layout {
      min-height: 100vh;
      display: grid;
      grid-template-columns: 1fr 1fr;
    }

    /* ── PANEL IZQUIERDO ── */
    .panel-left {
      background: var(--accent);
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      gap: 32px;
      position: relative;
      overflow: hidden;
      padding: 60px 40px;
    }

    /* Círculos decorativos (igual estructura que home, color azul) */
    .panel-left::before {
      content: '';
      position: absolute;
      width: 500px; height: 500px;
      border-radius: 50%;
      border: 1px solid rgba(255,255,255,0.06);
      bottom: -220px; right: -160px;
    }
    .panel-left::after {
      content: '';
      position: absolute;
      width: 280px; height: 280px;
      border-radius: 50%;
      border: 1px solid rgba(255,255,255,0.04);
      top: 50px; left: 30px;
    }

    /* Círculo adicional para profundidad */
    .panel-left-circle3 {
      position: absolute;
      width: 180px; height: 180px;
      border-radius: 50%;
      border: 1px solid rgba(255,255,255,0.05);
      bottom: 80px; left: -60px;
      pointer-events: none;
    }

    .panel-logo-area {
      position: relative;
      z-index: 1;
      text-align: center;
    }

    /* Badge "Business" sobre el logo */
    .business-badge {
      display: inline-flex;
      align-items: center;
      gap: 6px;
      background: rgba(255,255,255,0.12);
      border: 1px solid rgba(255,255,255,0.18);
      border-radius: 20px;
      padding: 5px 14px;
      font-family: var(--font-ui);
      font-size: 0.65rem;
      font-weight: 600;
      letter-spacing: 0.14em;
      text-transform: uppercase;
      color: rgba(255,255,255,0.85);
      margin-bottom: 24px;
    }
    .business-badge::before {
      content: '';
      display: inline-block;
      width: 6px; height: 6px;
      border-radius: 50%;
      background: var(--accent2);
    }

    .panel-wordmark {
      font-family: var(--font-hd);
      font-size: 3.2rem;
      font-weight: 700;
      letter-spacing: -0.03em;
      line-height: 1;
      margin-top: 20px;
    }

    .panel-tagline {
      font-size: 0.88rem;
      color: rgba(255,255,255,0.5);
      margin-top: 12px;
      letter-spacing: 0.01em;
      font-family: var(--font-ui);
    }

    /* Separador decorativo */
    .panel-divider {
      position: relative;
      z-index: 1;
      width: 40px;
      height: 1px;
      background: rgba(255,255,255,0.2);
    }

    /* Features list en el panel izquierdo */
    .panel-features {
      position: relative;
      z-index: 1;
      display: flex;
      flex-direction: column;
      gap: 12px;
      width: 100%;
      max-width: 260px;
    }

    .panel-feature {
      display: flex;
      align-items: center;
      gap: 12px;
      font-family: var(--font-ui);
      font-size: 0.8rem;
      color: rgba(255,255,255,0.65);
    }

    .panel-feature-icon {
      width: 28px; height: 28px;
      border-radius: 6px;
      background: rgba(255,255,255,0.10);
      display: flex; align-items: center; justify-content: center;
      flex-shrink: 0;
    }

    .panel-feature-icon svg {
      width: 14px; height: 14px;
      stroke: rgba(255,255,255,0.8);
    }

    /* ── PANEL DERECHO ── */
    .panel-right {
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 60px 48px;
    }

    .form-wrap {
      width: 100%;
      max-width: 380px;
      animation: fadeUp 0.7s cubic-bezier(0.16,1,0.3,1) both;
    }

    @keyframes fadeUp {
      from { opacity: 0; transform: translateY(20px); }
      to   { opacity: 1; transform: translateY(0); }
    }

    /* Logo pequeño en mobile */
    .form-logo {
      display: none;
      align-items: center;
      gap: 10px;
      margin-bottom: 28px;
    }

    .form-title {
      font-family: var(--font-hd);
      font-size: 1.75rem;
      font-weight: 600;
      letter-spacing: -0.02em;
      color: var(--ink);
      margin-bottom: 6px;
    }

    .form-sub {
      font-size: 0.85rem;
      color: var(--muted);
      margin-bottom: 36px;
    }

    .field { margin-bottom: 18px; }

    label {
      display: block;
      font-size: 0.68rem;
      font-weight: 500;
      letter-spacing: 0.1em;
      text-transform: uppercase;
      color: var(--ink2);
      margin-bottom: 8px;
      font-family: var(--font-ui);
    }

    input {
      width: 100%;
      background: var(--surface);
      border: 1px solid var(--border);
      color: var(--ink);
      font-family: var(--font-ui);
      font-size: 0.95rem;
      font-weight: 300;
      padding: 13px 16px;
      outline: none;
      transition: border-color 0.2s, box-shadow 0.2s;
      border-radius: 6px;
    }

    input:focus {
      border-color: var(--accent);
      box-shadow: 0 0 0 3px var(--accent-dim);
    }

    input::placeholder { color: var(--muted); }

    .btn {
      width: 100%;
      margin-top: 24px;
      padding: 14px;
      background: var(--accent);
      color: #fff;
      border: none;
      font-family: var(--font-ui);
      font-size: 0.8rem;
      font-weight: 600;
      letter-spacing: 0.12em;
      text-transform: uppercase;
      cursor: pointer;
      border-radius: 6px;
      transition: background 0.2s, transform 0.15s;
    }

    .btn:hover  { background: var(--accent-mid); }
    .btn:active { transform: scale(0.99); }
    .btn:disabled { opacity: 0.4; cursor: not-allowed; }

    .divider {
      display: flex; align-items: center; gap: 16px;
      margin: 24px 0;
    }
    .divider::before, .divider::after {
      content: ''; flex: 1; height: 1px; background: var(--border);
    }
    .divider span { font-size: 0.72rem; color: var(--muted); }

    .toggle { text-align: center; font-size: 0.82rem; color: var(--muted); }
    .toggle a { color: var(--accent); cursor: pointer; font-weight: 500; }
    .toggle a:hover { text-decoration: underline; }

    .msg { min-height: 18px; font-size: 0.78rem; text-align: center; margin-top: 10px; }
    .msg.error { color: var(--error); }
    .msg.ok    { color: var(--accent); }

    /* Link a finHub personal (footer del form) */
    .switch-product {
      margin-top: 36px;
      padding-top: 20px;
      border-top: 1px solid var(--border);
      text-align: center;
      font-size: 0.78rem;
      color: var(--muted);
    }
    .switch-product a {
      color: var(--accent);
      cursor: pointer;
      font-weight: 500;
      text-decoration: none;
    }
    .switch-product a:hover { text-decoration: underline; }

    /* ── RESPONSIVE ── */
    @media (max-width: 700px) {
      .layout { grid-template-columns: 1fr; }
      .panel-left { display: none; }
      .panel-right { padding: 52px 28px; align-items: flex-start; }
      .form-logo { display: flex; }
    }
  </style>
</head>
<body>

<div class="layout" id="layout" style="opacity:0;transition:opacity 0.3s">

  <!-- ── PANEL IZQUIERDO ── -->
  <div class="panel-left">
    <div class="panel-left-circle3"></div>

    <div class="panel-logo-area">
      <div class="business-badge">Business</div>

      <!-- Ícono: igual que finHub home pero en azul -->
      <svg width="96" height="80" viewBox="0 0 96 80" fill="none" xmlns="http://www.w3.org/2000/svg">
        <rect x="0"  y="52" width="20" height="28" rx="3" fill="rgba(255,255,255,0.35)"/>
        <rect x="25" y="34" width="20" height="46" rx="3" fill="rgba(255,255,255,0.55)"/>
        <rect x="50" y="8"  width="20" height="72" rx="3" fill="white"/>
        <circle cx="60" cy="24" r="12" fill="rgba(30,77,140,0.55)" stroke="white" stroke-width="1.5"/>
        <path d="M55 29 L60 19 L65 29" stroke="white" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" fill="none"/>
        <line x1="60" y1="19" x2="60" y2="29" stroke="white" stroke-width="2" stroke-linecap="round"/>
      </svg>

      <div class="panel-wordmark">
        <span style="color:rgba(255,255,255,0.55)">fin</span><span style="color:white">Hub.</span>
      </div>
      <div class="panel-tagline">Gestión financiera para PyMEs argentinas</div>
    </div>

    <div class="panel-divider"></div>

    <!-- Features -->
    <div class="panel-features">
      <div class="panel-feature">
        <div class="panel-feature-icon">
          <svg viewBox="0 0 24 24" fill="none" stroke-width="1.8"><path d="M12 2L2 7l10 5 10-5-10-5z"/><path d="M2 17l10 5 10-5"/><path d="M2 12l10 5 10-5"/></svg>
        </div>
        Multi-cuenta · Caja, banco, CC
      </div>
      <div class="panel-feature">
        <div class="panel-feature-icon">
          <svg viewBox="0 0 24 24" fill="none" stroke-width="1.8"><rect x="2" y="5" width="20" height="14" rx="2"/><path d="M2 10h20"/></svg>
        </div>
        IVA débito y crédito automático
      </div>
      <div class="panel-feature">
        <div class="panel-feature-icon">
          <svg viewBox="0 0 24 24" fill="none" stroke-width="1.8"><path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/><path d="M23 21v-2a4 4 0 0 0-3-3.87"/><path d="M16 3.13a4 4 0 0 1 0 7.75"/></svg>
        </div>
        Clientes, proveedores y CUIT
      </div>
      <div class="panel-feature">
        <div class="panel-feature-icon">
          <svg viewBox="0 0 24 24" fill="none" stroke-width="1.8"><polyline points="22 12 18 12 15 21 9 3 6 12 2 12"/></svg>
        </div>
        P&amp;L mensual y flujo de caja
      </div>
    </div>
  </div>

  <!-- ── PANEL DERECHO: FORM ── -->
  <div class="panel-right">
    <div class="form-wrap">

      <!-- Logo pequeño (solo mobile) -->
      <div class="form-logo">
        <svg width="28" height="24" viewBox="0 0 96 80" fill="none">
          <rect x="0"  y="52" width="20" height="28" rx="3" fill="#1e4d8c" opacity="0.4"/>
          <rect x="25" y="34" width="20" height="46" rx="3" fill="#1e4d8c" opacity="0.65"/>
          <rect x="50" y="8"  width="20" height="72" rx="3" fill="#1e4d8c"/>
          <circle cx="60" cy="24" r="12" fill="white" stroke="#1e4d8c" stroke-width="2"/>
          <path d="M55 29 L60 19 L65 29" stroke="#1e4d8c" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" fill="none"/>
          <line x1="60" y1="19" x2="60" y2="29" stroke="#1e4d8c" stroke-width="2.5" stroke-linecap="round"/>
        </svg>
        <span style="font-family:'Plus Jakarta Sans',sans-serif;font-size:1.3rem;font-weight:700;letter-spacing:-0.02em;">
          <span style="color:#8a8070">fin</span><span style="color:#1e4d8c">Hub.</span>
        </span>
        <span style="font-size:0.6rem;font-weight:700;letter-spacing:0.14em;text-transform:uppercase;color:#1e4d8c;background:rgba(30,77,140,0.08);border:1px solid rgba(30,77,140,0.18);border-radius:10px;padding:2px 8px;">BIZ</span>
      </div>

      <div class="form-title" id="form-title">Bienvenido</div>
      <div class="form-sub"   id="form-sub">Ingresá con tu cuenta empresarial</div>

      <div class="field">
        <label>Correo electrónico</label>
        <input type="email" id="email" placeholder="empresa@mail.com" autocomplete="email" />
      </div>
      <div class="field">
        <label>Contraseña</label>
        <input type="password" id="password" placeholder="••••••••" autocomplete="current-password" />
      </div>

      <div class="msg" id="msg"></div>
      <button class="btn" id="btn-login" onclick="handleLogin()">Ingresar</button>

      <div class="divider"><span>o</span></div>
      <div class="toggle" id="toggle-text">
        ¿No tenés cuenta? <a onclick="toggleMode()">Registrate gratis</a>
      </div>

      <!-- Link a finHub personal -->
      <div class="switch-product">
        ¿Buscás finanzas personales?
        <a href="https://finhub-app-fb853.web.app" target="_blank">Ir a finHub Home →</a>
      </div>

    </div>
  </div>

</div>

<script type="module">
  import { initializeApp }
    from "https://www.gstatic.com/firebasejs/10.12.0/firebase-app.js";
  import { getAuth, signInWithEmailAndPassword, createUserWithEmailAndPassword, onAuthStateChanged }
    from "https://www.gstatic.com/firebasejs/10.12.0/firebase-auth.js";

  // ─── CONFIG FIREBASE — proyecto exclusivo finHub Business ───
  const firebaseConfig = {
    apiKey:            "AIzaSyDEdKUEJighBOwemKjh0fMxr-TGN2U9Ulo",
    authDomain:        "finhub-business.firebaseapp.com",
    databaseURL:       "https://finhub-business-default-rtdb.firebaseio.com",
    projectId:         "finhub-business",
    storageBucket:     "finhub-business.firebasestorage.app",
    messagingSenderId: "946602595680",
    appId:             "1:946602595680:web:0ebeacaa6c499b21f0fa05"
  };
  // ─────────────────────────────────────────────────────────────

  const app  = initializeApp(firebaseConfig);
  const auth = getAuth(app);

  // Si ya está logueado, ir directo al dashboard
  onAuthStateChanged(auth, user => {
    if (user) {
      window.location.href = 'app.html';
    } else {
      document.getElementById('layout').style.opacity = '1';
    }
  });

  let isRegister = false;

  window.toggleMode = () => {
    isRegister = !isRegister;
    document.getElementById('btn-login').textContent  = isRegister ? 'Crear cuenta' : 'Ingresar';
    document.getElementById('form-title').textContent = isRegister ? 'Crear cuenta' : 'Bienvenido';
    document.getElementById('form-sub').textContent   = isRegister
      ? 'Registrá tu empresa para comenzar'
      : 'Ingresá con tu cuenta empresarial';
    document.getElementById('toggle-text').innerHTML  = isRegister
      ? '¿Ya tenés cuenta? <a onclick="toggleMode()">Ingresá</a>'
      : '¿No tenés cuenta? <a onclick="toggleMode()">Registrate gratis</a>';
    setMsg('');
  };

  window.handleLogin = async () => {
    const email    = document.getElementById('email').value.trim();
    const password = document.getElementById('password').value;
    const btn      = document.getElementById('btn-login');
    if (!email || !password) { setMsg('Completá todos los campos.', true); return; }
    btn.disabled = true; setMsg('');
    try {
      isRegister
        ? await createUserWithEmailAndPassword(auth, email, password)
        : await signInWithEmailAndPassword(auth, email, password);
      // onAuthStateChanged redirige automáticamente
    } catch (e) {
      btn.disabled = false;
      const msgs = {
        'auth/user-not-found':       'No encontramos esa cuenta.',
        'auth/wrong-password':       'Contraseña incorrecta.',
        'auth/email-already-in-use': 'Ese email ya está registrado.',
        'auth/weak-password':        'Mínimo 6 caracteres.',
        'auth/invalid-email':        'Email inválido.',
        'auth/invalid-credential':   'Credenciales inválidas.',
      };
      setMsg(msgs[e.code] || e.message, true);
    }
  };

  window.setMsg = (t, err = false) => {
    const el = document.getElementById('msg');
    el.textContent = t;
    el.className = 'msg ' + (err ? 'error' : 'ok');
  };

  // Enter en contraseña dispara el login
  document.getElementById('password').addEventListener('keydown', e => {
    if (e.key === 'Enter') window.handleLogin();
  });
</script>
</body>
</html>
